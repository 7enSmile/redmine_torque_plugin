<%= 'Project: ' %>
<%= select_tag 'settings[project]', options_for_select(Project.all.collect{ |u| [u.name, u.id] }, selected: if Setting.plugin_redmine_torque_plugin then settings['project'] else 1 end) %>
<br>
<%= 'Assignee: ' %>
<%= select_tag 'settings[user_assignee]', options_for_select(User.all.collect{ |u| [u.lastname, u.id] }, selected: if Setting.plugin_redmine_torque_plugin then settings['user_assignee'] else 1 end) %>

